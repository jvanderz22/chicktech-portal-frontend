
<form class="form-vertical">
  <fieldset>
    <legend> New Project </legend>

    {{#form-group label="Title"}}
      {{input value=model.title}}
    {{/form-group}}

    {{#form-group label="Project Coordinator"}}
      {{input value=model.projectCoordinator}}
    {{/form-group}}

    {{#form-group label="Description"}}
      {{textarea
        value=model.description
        class="small-text"
        cols=40
        rows=10
      }}
    {{/form-group}}

    {{#form-group label="Start Date"}}
      {{input value=model.startDate type="date"}}
    {{/form-group}}

    {{#form-group label="End Date"}}
      {{input value=model.endDate type="date"}}
    {{/form-group}}

    {{#form-group label="Volunteers Needed"}}
      {{input value=model.volunteersNeeded type="number"}}
    {{/form-group}}
  </fieldset>

  <button {{action 'submit'}} class="btn btn-primary" type="submit">
    Submit
  </button>
</form>
